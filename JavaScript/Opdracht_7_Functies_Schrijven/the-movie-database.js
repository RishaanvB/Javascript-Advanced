favoriteMovie = {
    title: 'Frozen',
    duration: 120, 
    stars: ['Elsa ',' Anna',' Olaf']
}

const movieTitle = favoriteMovie.title
const movieDuration = favoriteMovie.duration
const movieStars = favoriteMovie.stars

console.log(movieTitle + ' lasts for ' + movieDuration + ' . Stars: ' + movieStars );